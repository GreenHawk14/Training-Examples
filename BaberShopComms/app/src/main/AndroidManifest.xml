<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.babershopcomms">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaberShopComms">
        <activity android:name=".CustomerVerify"></activity>
        <activity android:name=".Customerphoneauth" />
        <activity android:name=".NewClientUser" />
        <activity android:name=".BarberAssignment" />
        <activity android:name=".StylesHaircutscreen" />
        <activity android:name=".NewBarberAccount" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".StartMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>